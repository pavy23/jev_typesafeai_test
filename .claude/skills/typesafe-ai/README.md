Vendored copy of the official TypeSafe agent skill (plugin `typesafe@typesafe-ai` v0.5.7,
https://github.com/typesafe-ai/skills, MIT). Kept here so cloud / fresh sessions of this repo
have the skill even before `claude plugin install typesafe@typesafe-ai` has run locally.
Invoke as `/typesafe-ai` (project skill) or `/typesafe:typesafe-ai` (plugin). Refresh by re-copying
skills/typesafe-ai/SKILL.md from the upstream repo.
